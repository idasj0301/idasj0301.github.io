# 船客官网 · 操作指南（从这里开始）

项目目录：`官网/chuanke-site/`

---

## 第一步：在本机跑起来（约 5 分钟）

在终端执行（不要用中文路径出问题的话，可直接复制）：

```bash
cd "/Users/pro/Documents/官网/chuanke-site"
npm install
cp .env.example .env
npm run dev
```

浏览器打开终端里提示的地址（一般是 **http://localhost:4321**）。

**若 `npm install` 失败**，按报错处理：

```bash
# 权限：npm 缓存目录属主不对（EPERM）
sudo chown -R "$(whoami)" ~/.npm

# 建议使用 Node 20 LTS（你若是 Node 24 可能触发 esbuild -88）
nvm install 20
nvm use 20

cd "/Users/pro/Documents/官网/chuanke-site"
rm -rf node_modules package-lock.json
npm install

# 若仍是 esbuild -88
xattr -cr node_modules
npm rebuild esbuild
npm run build
```

或临时用项目内缓存安装：

```bash
npm install --cache ./.npm-cache
```

---

## 第二步：配置企业微信（转化）

1. 向销售要 **企微获客链接**（或顾问二维码图片）。
2. 编辑 `.env`：

```env
PUBLIC_WECOM_URL=https://work.weixin.qq.com/ca/你的链接
```

3. 把二维码图片保存为：`public/advisor-qr.png`（可选，顾问页会显示）。

保存后 **重启** `npm run dev`。

---

## 第三步：从企微 PDF 录入一条真实航程

1. 从企微尾盘下载 1 份 PDF，复制全文。
2. 在 Cursor 里对我说：

   > 这是南极 XX 团的 PDF 正文，请按 SKU_TEMPLATE 生成一条 JSON，slug 用 xxx

3. 打开 `data/products.json`，把生成的那条 **合并进数组**（或替换对应样板）。
4. 校验：

```bash
npm run validate:products
```

5. 刷新浏览器，打开 `/trips/你的slug` 看详情。

**首期上线**：把要展示的条目设 `"published": true`，暂不卖的设 `"published": false`。

字段说明见：[data/SKU_TEMPLATE.md](./data/SKU_TEMPLATE.md)

---

## 第四步：批量铺 30+ 条

| 你做 | 说明 |
|------|------|
| 导出 PDF 清单 | 企微尾盘按文件名记下 slug |
| 每次 3～5 条 | 粘贴正文给我 → 生成 JSON → 你校对价格/日期 |
| `published` | 主推 true，售完/尾盘 false |

可重置 32 条样板（仅测试用）：

```bash
node scripts/generate-products.mjs
```

---

## 第五步：上线预览（给老板看）

**海外快速预览（推荐先做）**

1. 代码推到 GitHub（仓库根目录选 `chuanke-site` 或整仓）。
2. [vercel.com](https://vercel.com) → Import 项目。
3. Root Directory：`chuanke-site`
4. Build：`npm run build`，Output：`dist`
5. 环境变量：`PUBLIC_WECOM_URL` = 同 .env
6. 部署后访问预览域名。

详细 checklist：[DEPLOY.md](./DEPLOY.md)

---

## 目录速查

| 路径 | 作用 |
|------|------|
| `data/products.json` | 全部航程 SKU |
| `data/articles.json` | 船说文章 |
| `src/pages/` | 页面（一般不用改结构） |
| `public/llms.txt` | 构建前自动生成，给 AI 抓取 |
| `操作指南.md` | 本文件 |

---

## 和我协作的固定话术

- **录入 SKU**：「PDF 正文如下…，品类南极，请生成 products.json 一条」
- **改样式**：「详情页字号再大一点 / 顶栏移动端改成底部导航」
- **上线卡住**：「build 报错完整日志如下…」

---

## 当前数据状态

- 共 **32** 条样板 SKU（脚本生成，**需用 PDF 替换文案**）
- 其中 **13** 条 `published: true` 可在站内看到
- 上线前务必校对价格与出发日期
